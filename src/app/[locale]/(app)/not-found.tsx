import { NotFoundPanel } from "@/components/shared/not-found-panel";

/** Unknown route inside the shell — navigation stays available. */
export default function AppNotFound() {
  return <NotFoundPanel />;
}
